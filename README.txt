Wikipedia-English-Search

A Search engine on whole English XML dump of Wikipedia

- Used Weighted tf-idf Model for Page ranking
- Stored inverted index with document frequency

How to compile: 
- open command prompt
- install.bat
- run.bat path < inputfile (file consisting of queries , put the generated index on the mentioned "path")
